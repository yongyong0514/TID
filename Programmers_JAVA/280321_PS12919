class Solution {
    public String solution(String[] seoul) {
        
        String answer = "";
        
        for(int i = 0; i < seoul.length; i++) {
        	
            //.equals()는 비교하고자 하는 내용이 같은지 검사하는 메소드입니다.
            //Object 클래스의 메소드이므로 모든 객체에서 사용할 수 있습니다.
            //target1.equals(target2) 형식으로 사용합니다.
            //.equals()와 다르게 '==' 연산자는 두 객체가 같은 것을 가리키는 주소만 참조합니다.
            if(seoul[i].equals("Kim")) {
                
                answer += "김서방은 " + i + "에 있다";
            }
        
        }
        
        return answer;
    }
}
